{
  "name": "spotify-queue-app",
  "version": "1.0.0",
  "description": "Self-hosted Spotify queue management for events",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:admin\"",
    "dev:server": "nodemon server/index.js",
    "dev:client": "cd client && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "build": "cd client && npm run build && cd ../admin && npm run build",
    "build:demo": "cd client && npm run build:demo && cd ../admin && npm run build:demo && cd .. && node scripts/build-demo.mjs",
    "build:demo:local": "cd client && npm run build:demo:local && cd ../admin && npm run build:demo:local && cd .. && node scripts/build-demo.mjs",
    "preview:demo": "node scripts/serve-demo.mjs",
    "dev:demo:client": "cd client && npm run dev:demo",
    "dev:demo:admin": "cd admin && npm run dev:demo",
    "start": "node server/index.js",
    "install:all": "npm install && cd client && npm install && cd ../admin && npm install"
  },
  "keywords": [
    "spotify",
    "queue",
    "event"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "better-sqlite3": "^9.2.2",
    "better-sqlite3-session-store": "^0.1.0",
    "cn": "^0.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-session": "^1.19.0",
    "react": "^19.2.8"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}
